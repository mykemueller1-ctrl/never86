# Never 86'd — Cursor / any agent

Public site: https://www.never86.ai
MCP (add once in Cursor → Settings → MCP): `https://www.never86.ai/api/mcp`
Then call `get_operator_system`, `get_operator_logic`, `get_3p_audit_logic`, `list_answers`, `list_free_agents`.

## Product
- Promise: Find the leak. Assign the fix. Keep the receipt.
- **One location + one seat is free.** Extra seats/locations paid.
- That free seat goes **past MarginEdge** (they charge ~$350/loc for invoices + prime cost). We do the operator job: yesterday → one action → night proof. Z, voids, labor, tips, invoices, vendors, beverage, catering, 3P, shift.
- **3P is the Google door, not the company.** Homepage is the OS. `/trial` = seat. `/audit` = 60-second DoorDash proof.
- We are not R365's GL. We are not Voosh (no merchant portal login).
- LLM ranks the move. Operator hits send. No auto-mail. No names as thieves. Incomplete week stays Open. No count → no food cost.

## Do not
- Put Community Tap / CTap private numbers, names, or mail on this site.
- Guarantee recovery dollars.
- Shrink the brand to a fee calculator to rank faster.
- Start a fourth Never86 repo. If this git is not what deploys www.never86.ai, STOP and say so.

## Active job
See `docs/CURSOR-GTM.md` and GitHub issue #122. GSC started indexing 2026-08-21. Ship unique titles, robots.txt fix, home CTA (one seat free), `/for/owner` → `/trial`.
