# Daily board — 2026-08-30 (after 10:46 PM CT 8/29)

**Myke:** “get everything pushed — you have authorization.”  
**This agent:** pushed HQ truth + live proofs. Did **not** apply Neon or flip staff login (SQL is draft; roster/emails still missing). Did **not** submit four LLM directories (needs your store logins).

## LIVE on www.never86.ai (probed 11:18 PM CT)

| Surface | Status |
|---|---|
| Production deploy | `115b7b6` · Vercel Production 02:10 UTC |
| Home | 200 · “one seat” + `/trial` + `/audit` |
| `/trial` `/audit` `/for/owner` `/action-shift` | 200 |
| `/llm-shells` `/privacy` `/terms` | 200 · directories still **not submitted** |
| `/staff/desk` `/staff/login` `/staff/claim` | pages 200 |
| `POST /api/staff/login` | **503 fail-closed** (correct) |
| MCP `https://www.never86.ai/api/mcp` | **v3.1.0** · 16 tools · initialize + `get_operator_system` + `get_hunter_standup` 200 |

## Merged tonight (no open PRs left)

`#168` inbound close mail · `#169` hunter MCP · `#170` one-seat CI so hunter can ship

## WAITING ON YOU (desktop — 2 clicks)

1. **Neon:** apply `sql/0005_staff_seat_auth.sql` only after Tom/Myke roster + work emails exist. Then Vercel env `STAFF_SEAT_LOGIN_ENABLED=true` + `DATABASE_URL`.
2. **Four LLM stores:** ChatGPT Connectors / Claude Connectors / Gemini Gem / Grok MCP — paste `https://www.never86.ai/api/mcp`. Listing packet is live; human submit still required.

## IN FLIGHT / parked

- `#118` self-serve free seat — schema/security still the Monday gate
- `#122` GTM — pages live; issue still open
- `#153`/`#154` four-LLM *provider* proof — public MCP works; store installs unverified
- Esteban shortlist — his court
- Social clip factory — draft only

## Hard stops still on

No passwords/2FA. No auto-post. No `#162`/`#167` reopen. No CTAP dollars on GTM.
