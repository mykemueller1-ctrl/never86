# Intake — grok-shareable-86-swarm

**From:** Cursor factory `bc-ee4c1774` + research swarm `bc-16edf4c0`, `bc-ed7a0a8a`, `bc-a564c62a`  
**Status:** drafted / tested in PR — not live-desk, not merged, not deployed  
**For HQ:** Grok command hub

## What I found

- 157 bots.new shareables, 36 gbt.grok.me, 162 ShareGrokBots, 253 grokbot.dev templates, 10 Grokyard, 5 grokbots.space
- **Zero** public bots named or described as Never86 / branded 86
- Useful matches are orchestrators, invoice capture, Cursor loops, Reddit hunter, short-form scout
- Share copies config only — never keys or the shared computer

## Files created/changed

- `config/grok-shareable-swarm.json`
- `src/lib/grokShareableSwarm.ts` + test
- `docs/company/grok-bots/*`
- Product role `grok-shareable-scout` in `companyOrg.ts`
- Cursor automation §7

## Open loops for Myke

- Inspect and optionally Add the mapped `x.ai/bot` links
- Desktop MCP Connect for Gmail / Calendar / X if still `needsAuth`
- Do not create `XAI_API_KEY` unless a later job needs the model API

## Do NOT do from cloud

- Add bots to the live desk
- Paste or invent API keys
- Merge, deploy, CRM write, or social send
