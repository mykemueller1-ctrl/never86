# Codex → Grok Hub / Cursor Factory Handoff

**Updated:** 2026-08-30 7:03 AM CT
**Owner:** Grok Chat command hub
**Purpose:** durable decisions and verified work state shared by Grok, Cursor, Grok Bots, and Codex; not a raw transcript

## Current decisions

- Myke's single day-to-day conversation and command hub is the Grok project **Command center from never 86**, chat **Cursor agents: You talk, I run** (`3f690a7c-b2e5-4101-be38-89cc8b5df3f6`). Myke should not shuttle packets, files, or full conversations between tools.
- Grok is Myke's second brain and router. It turns Myke's requests into owned jobs for Cursor agents, Grok Bots, and other approved workers, then returns one result or approval inbox to the same Grok chat.
- Cursor is the code and execution factory. It reads the canonical repository, launches and manages coding agents, makes isolated branches, tests, builds, and prepares deploys. Cursor is not another founder inbox.
- Codex is the orchestration, command, and audit layer above Grok, Cursor, and Grok Bots. It verifies evidence, detects stalled/duplicated/drifting work, issues correction packets where an authorized path exists, and alerts Myke only for material wins, failures, blockers, or decisions.
- The Codex thread **Connect Cursor to MCP and Grok** (`01a03584-d96b-74a2-ab13-62a62911e416`) is the watchtower and repair room, not Myke's daily operating chat. The duplicate **Check Grok build status** thread was consolidated into it and stopped.
- Keep Grok, Cursor, and their agents moving on concrete owned work. Routine implementation choices do not wait on Myke; stop only for a real decision, missing private input, permission boundary, or external blocker.
- Myke authorizes Grok to keep the next safe bounded job queued and dispatch it automatically when the current worker exits with evidence. Grok does not need to ask Myke between routine isolated-branch/PR jobs or approved read/draft jobs. Production merges/deploys, live migrations, secrets, spend, publishing/sending, CRM writes, and permission expansion remain human gates.
- At 7:10 PM CT on 28 Aug, Myke explicitly approved moving Community staff authentication to a live staff database and enabling Google, email, X, and later phone sign-in. This is goal/release authorization, not permission to apply the current `sql/0005_staff_seat_auth.sql` unchanged: that file is a readiness draft with `mail_sent=false`, `live_issuance='blocked'`, and routes that intentionally return `503/403`. First produce a reviewed activation migration and working current-main auth PR. Stage Google + email first; X and phone require provider credentials/callbacks in approved secret storage. Never invent credentials or paste secrets into chat/git.
- At 7:35 PM CT Myke fixed the CTAP staff-onboarding rule: a new worker proves identity with email, phone, X, Google, or another explicitly supported verified provider; verification creates a **pending CTAP access request**, not a staff seat and never operator/admin access. The request goes into one approval inbox visible to Myke and Tom. Either approver can match the person to the live CTAP roster, assign the approved role/department/seat and checklist/schedule scope, or reject the request. Until approval, the user sees only a pending/rejected status and no CTAP private data. Record provider, verified identifier hash, requester, approver, assigned role, decision time, revoke/reset history, and immutable audit receipt. Support account linking without duplicate people; require re-verification for identifier changes. Email/Google is the first implementation tranche; phone and X may appear only when real provider configuration is proven.
- Myke's one-seat doctrine is locked: one human has one canonical Never86 backend identity/seat, tenant, role, permissions, data boundary, and audit trail. ChatGPT, Claude, Gemini, and Grok are thin shells into that same seat, never four disconnected accounts. Account linking, Myke/Tom approvals, role assignment, data/tool access, LLM/tool calls, evidence receipts, external-write gates, failures, revokes, and admin changes must all resolve to and be auditable against that canonical seat.
- **Privacy incident first gate done:** PRs `#162` and `#167` were redacted and **closed without merge** at 5:19 PM CT. Do not reopen or merge either. Public `GROK_HANDOFF.md` store-private dollars were stripped in `610f2c3` on `cursor/desktop-computer-auth-dcde`. That redaction is not yet a current-`main` PR. History rewrite / GitHub Support cache cleanup remains a later gate. No credential/secret pattern is proven; rotate nothing without new evidence.
- Community Tap & Pizza is the private reference tenant and onboarding lab. Every proven workflow must become a reusable operator skill, agent, connector, checklist pack, or install step instead of remaining Community-only prompt folklore.
- **PDQ menu drift — parked, not now.** `UNKNOWN`/`UKNOWN`, Large Pizzas, and negative/neg menu lines are menu drift. For mix reads, roll them into **Food**. Do not start a PDQ button/mapping cleanup until the current build is done. Then come back and fix the POS categories. Not a person, not theft, not tonight’s job.
- Never86 scales through bounded specialist agents with an owner, queue, budget, stop condition, evidence contract, and escalation path. “Thousands of bots” does not mean uncontrolled loops, duplicate work, fake activity, or agents with broad write authority.
- The product has one Never86 MCP/backend and evidence contract, with thin install packages for ChatGPT/OpenAI, Claude, Gemini, and Grok. Public read-only tools remain separate from private tenant tools; private tools require tenant OAuth, least privilege, audit receipts, and human approval for external side effects.
- Claims such as built, tested, pushed, merged, and deployed require visible evidence. Never report one state as another.
- Never86 Operator System MCP is connected at `https://www.never86.ai/api/mcp` and provides the operating logic.
- Sentia+ at `https://your.sentia.plus/` is the CRM source of truth. Apollo is not used.
- Sentia+ is currently operated through its signed-in browser app because this setup has no public Sentia MCP endpoint.
- **“Grok Bots” means the separate Grok Bot desktop workspaces with their own persistent Computer and Routines** (for example `LinkedIn as Myke`, `Facebook Never86'd + CTap`, `TikTok as Myke + Never86'd`, `X as Myke`, and the build/operations workspaces). It does not mean ordinary Grok Web automations or cards inside the command-hub chat.
- At 8:35 PM CT on 28 Aug, Myke authorized workers to use connected apps, tools, Google Chrome, and desktop computer control whenever a real Never86 job needs them. Workers must tell Myke the exact click (MCP Connect, Agent Computer takeover, Beta egress) instead of stalling. This does **not** authorize random account creation, password/2FA entry by an agent, auto-send, Facebook work, Apollo, or Mac takeover from a cloud agent.
- All of Myke's Grok Bots share one persistent managed Linux cloud computer, browser sessions, files, and logins. That computer is not Myke's local Google Chrome profile, and separate Bots are not separate security boundaries. Prefer Plugins/MCP; for a site without a connector, the Bot stops at authentication and Myke takes over Agent Computer to enter a password, passkey, 2FA code, or CAPTCHA once, then returns control. Never paste credentials into bot chat. A Cursor **cloud** agent cannot drive that computer or Myke's local Chrome.
- Some websites block the shared computer because its traffic comes from a managed/datacenter address. When available, the supported mitigation is Grok Bot **Settings → Beta** egress routing through Myke's computer; do not bypass a site's safeguards. Update Agent Computer before considering Reset, because Reset can lose recent unsynced work.
- The actual Grok Bot workspaces are being expanded into a full social department covering intelligence, editorial planning, X/LinkedIn, short-form video, Facebook/community, Reddit/forums, repurposing, publishing queue, and performance learning.
- Social bots do the large research, drafting, repurposing, queue, and learning workload. Myke remains the release gate for posts, replies, DMs, publishing, spending, production changes, and CRM writes.
- At 4:57 PM CT on 29 Aug, Myke ordered recurring Never86 founder/product video creation to move into the xAI Grok Bot system as a reusable template. The safe operating default is locked for `X as Myke`: one daily 7:00 AM America/Chicago draft/render batch with exactly three materially distinct creatives (conflict, identity, payoff), a 30-day novelty check, source-truth gates, an approval card, and performance-learning fields. Publishing, replies, DMs, boosts, and spend stay disabled until Myke releases a specific asset. The canonical contract is `docs/company/social/GROK_BOT_CLIP_FACTORY.md`; the Routine spec is `docs/company/social/GROK_ROUTINE_X_DAILY_DRAFT.md`; the reusable Markdown and JSON contracts are under `docs/company/social/templates/`; the first new batch is `docs/company/social/batches/2026-08-29-founder-product-clips.md`.
- At 7:03 AM CT on 30 Aug, Myke locked the **Never86 Bolt** go-to-market thesis: the Reel is the storefront; social creates demand; a restaurant-owned checkout opens with the smallest possible in-app-browser handoff; Bolt routes the order and coordinates delivery; the operator keeps the brand, guest relationship, data, menu, and operating rules. Third-party marketplaces become controlled overflow, not the front door. A verified 55-second internal concept demo, build brief, source manifest, platform limits, and MVP proof plan now exist; canonical intake is `docs/company/intake/2026-08-30-never86-bolt-social-to-order.md`. This is a concept lane, not a live integration claim. TikTok hot-food native Shop checkout is not proven/currently supported; use eligible Destination Links or profile/order actions into a fast web checkout until partner/API support is verified. Public/commercial use of Eric's Family Barbecue or Dayne's Craft Barbecue excerpts requires owner permission. No post, partner outreach, deploy, or production dispatch was authorized.
- Myke explicitly deferred Facebook on 26 Aug. Do not touch the Facebook Grok Bot or treat its login as a blocker for the Cursor/Grok repair queue; revisit Facebook only when Myke asks.
- Do not paste full AI conversations into Cursor, Sentia+, or company docs. Preserve useful facts, decisions, evidence, drafts, and open loops.
- Myke wants one **Daily Myke Handoff & Tomorrow Brief** workflow instead of ad hoc updates. Grok owns the master packet: verified actions, decisions, commitments, unfinished work, and tomorrow's calendar/attendees/prep. It then produces need-to-know recipient slices for Kristin and Vadim. The workflow drafts automatically but does not send until Myke approves the exact recipients, channel, and copy.

## Current routing

1. Myke speaks only in the Grok command-hub chat for ordinary work.
2. Grok keeps context, ranks the work, and dispatches owned jobs to Cursor agents and Grok Bots.
3. Cursor reads this handoff plus the canonical repository, executes code work, and returns commit/test/push/deploy evidence.
4. Grok Bots run their scheduled research, operations, meeting-prep, social, and evidence jobs and return one consolidated Grok approval packet.
5. Grok assembles the Daily Myke Handoff & Tomorrow Brief, refreshes it before the first next-day meeting, and returns one approval card to Myke. Approved sends use separate Kristin and Vadim versions and retain a delivery receipt.
6. Codex watches the chain every 30 minutes, verifies material claims, and reports only meaningful drift or failures. It does not become a second daily inbox.

## Never86 agent organization

- **Myke — CEO/release gate:** decides priority changes, production deploys/migrations, spend, external sends/posts/CRM writes, real CTAP staff-role approval, and performs password/2FA/CAPTCHA sign-ins. Routine implementation does not wait on Myke.
- **Codex — Watchtower/architecture:** defines acceptance gates, audits evidence and security, detects duplicate/stalled/drifting agents, updates the durable handoff, and issues one correction packet. Codex does not claim worker output is shipped without Git/build/live proof.
- **Grok — Command hub/dispatcher:** owns the ranked queue, gives every worker one bounded job, keeps the next safe job ready, and returns one approval or blocker inbox to Myke.
- **Cursor — Code factory:** one code-writing cloud slot until three consecutive bounded jobs finish with the right repo, clean branch, tests/build, and pushed PR evidence. Then consider cap `2`; do not jump from one unreliable slot to uncontrolled scale.
- **Product lane:** Community/Action Shift onboarding, manager workflows, checklists, PDQ/vendor/prime-cost evidence, and reusable operator packs.
- **Integration lane:** provider-neutral contracts plus POS, scheduling, checklist, invoice, delivery-marketplace, and CRM adapters; research and synthetic fixtures may run in parallel, live credentials may not.
- **Quality lane:** security, tenant isolation, negative tests, evals, dependency/build/deploy evidence, and stranger-walk acceptance. This lane cannot approve its own production release.
- **Agent/knowledge lane:** one versioned skill/knowledge source and eval set, packaged as thin OpenAI, Claude, Gemini, and Grok shells over the same OAuth MCP backend.
- **GTM/Sentia+ lane:** market research, ICP, lead qualification, and draft sequences. Sentia+ stays the CRM source of truth; no CRM writes or outreach without approval.
- **Victor/media/social lane:** guest research, briefs, podcast/clip plans, channel drafts, and performance learning. Victor owns interview/creative quality; humans approve sending and publishing.
- **Brand/source-of-truth lane:** a parallel read/draft-only owner audits `never86.ai`, four-LLM surfaces, social, podcast/video, documents, and sales media, then returns one source of truth for logo/image treatment, colors, typography, accessibility, plain-human voice, terminology, reusable templates, and an evidence-backed gap map. No overlapping code edits or publishing.
- Every bot needs: one owner, one queue item, allowed tools/data, budget/timebox, acceptance evidence, stop condition, and escalation reason. Research/draft bots may work in parallel; code writers may not edit overlapping scope.

## Honest connection state

- Grok is connected to the public read-only Never86 Operator System MCP at `https://www.never86.ai/api/mcp`.
- The authenticated private Grok → Cursor bridge is **merged, deployed, credentialed, OAuth-connected, and live-tested**. PR `#125` merged the MCP bridge; PR `#126` added Grok-compatible OAuth/PKCE. Production release is `84e543a02881b3e0df2cd298b03ef93ba7c4318a` at `https://www.never86.ai/api/orchestrator/mcp`.
- Production `main` is now `678a96ea614dc34da36fa92c8bfe480b32e6fd8d` after PR `#165` merged at 21:02 UTC despite its audit-only/no-merge gate; GitHub CI, IndexNow, and Vercel are green. The earlier PR-166 public-data exposure remains in history/current main. Schedule/Time-Off plus CTAP bar-week extras are deployed, while `POST /api/staff/login` remains live-verified fail-closed at `503` and no credential has been issued. PR `#161` is finally closed as superseded. The public four-LLM try door remains the current direction; live provider installations and marketplace submissions remain unverified/not submitted.
- The four provider shells are **deployed as backend/API surfaces plus a public indexed try door**. Live installation inside ChatGPT, Claude, Gemini, and Grok remains unverified; marketplace submissions remain not submitted. The stale `drafted-in-git` claim is removed. The page is intentionally advertised in the sitemap and LLM files under current direction, but that does not prove any provider-side installation.
- Grok has the connected custom connector **Never86 Cursor Factory** with exactly four tools: `cursor_list_agents`, `cursor_get_agent`, `cursor_prepare_dispatch`, and `cursor_launch_agent`. The legacy public business-logic connector **Never86 Operator System v3** remains separate at `/api/mcp`.
- Live evidence: OAuth authorize returned `302`, token exchange `200`, authenticated MCP `200`, and the Grok hub itself called `cursor_list_agents` successfully. Grok returned five live agents and explicitly reported that nothing was prepared, launched, published, or sent.
- Autonomous launch is enabled server-side with `CURSOR_MAX_ACTIVE_AGENTS=1`. PR `#165` merged to production `main` at `678a96e`. PRs `#162` and `#167` are closed without merge. One-seat claim is drafted at `610f2c3` on `cursor/desktop-computer-auth-dcde`; a current-`main` PR is still required. Do not relaunch those leak workers.
- The OAuth release passed all 16 test files / 125 tests, ESLint, production Next build, GitHub CI, Vercel preview, and a production dependency audit with 0 vulnerabilities.
- Actual Grok Bot audit at 10:00–10:20 PM CT: LinkedIn's hourly routine is Active with recent `Succeeded` trigger history; the Facebook channel's hourly routine is Active with recent `Succeeded` trigger history; the general operations bot's hourly CTap source pulse is scheduled; `Facebook Never86'd + CTap` is blocked on Facebook sign-in, with its weekday pack scheduled and another Facebook routine paused; the general bot's daily Facebook 3P scan is paused; TikTok's routine is paused; X had no recurring Routine at audit time. As of 29 Aug, the X daily clip Routine contract/templates are built, but xAI workspace enablement, first trigger, and playable render evidence remain unverified. A `Succeeded` routine proves the trigger ran, not that social work completed.
- PR `#138` is the completed onboarding-security reconciliation result. It **merged to the safe branch only** at merge commit `27d637b51c45922643b62f123569a0da9c5e1db5` on 26 Aug 22:08 UTC. It is not production-deployed and no live Neon apply has been verified. PRs `#135` and `#137` are closed as superseded audit history.
- The scheduled 30-minute Codex watchtower is producing successful heartbeat checks again. Chrome access recovered at 7:00 AM CT on 27 Aug. Codex submitted the authorized CTAP repair order in the Grok command hub, Grok detected an existing cap-1 repair worker and correctly avoided a duplicate launch, and Cursor/GitHub evidence was then verified directly. Continue treating Grok/Cursor browser state as inaccessible whenever the admin-policy check is unavailable; never bypass the control.
- AEO / issue `#122` **merged** as PR `#139` on 27 Aug 23:51 UTC. No new search-page work.
- At 2:10 AM CT, Grok cleared the cap-1 slot by status and launched `ctap-manager-proof-ui`: worker `bc-3a1b9237-0cc6-4501-8e1e-c2ee7dbaa271`, run `run-f23c8a6b`, on `mykemueller1-ctrl/never86` from `codex/action-shift-122-safe` at `27d637b51c45922643b62f123569a0da9c5e1db5`. Cursor independently shows the exact bounded task and `Planning next moves`. It owns manager-first UI, checklist ownership/proof, exceptions, and tenant boundary with synthetic roles only. No merge, deploy, live migration, secrets, CRM/social write, or Facebook.
- That worker produced open PR `#140`, branch `cursor/never86-ctap-manager-proof-ui-ac97`. Automated review found two P2 correctness defects at the original head `db1cc438ca09d8530a58742078245d9d1b358050`: ignored per-step `escalationMinutes` grace and double-counted overdue escalations. Grok routed one repair worker, `bc-205f1bcb-693c-41c1-a86d-3bbdf99f0cac` / `run-6c56029a`, which repaired the same PR in place at `597b23456763fbc1ad62769e4434d829ef6b832e`. The implementation now uses `dueAt + step.escalationMinutes`, counts one overdue incident once, and includes focused 20-minute driver-grace and single-count regression tests. Cursor reports Vitest 235/235 across 32 files, ESLint green, and Next production build green; GitHub `verify` and the Vercel preview are independently green. The worker is terminal. PR `#140` remains open, unmerged, not production-deployed, and not live-verified.
- Four-model worker `bc-12a88940-2996-457c-b408-af46e532a49b`, run `run-467f7588-449d-4262-8e43-792f50967a9c`, produced PR `#141` from branch `cursor/never86-four-llm-shells-8668`. Head `195da90ae5f50ee772b58a73a8447a7893a39c3a` contains one provider-neutral skill pack, thin ChatGPT/Claude/Gemini/Grok shells, shared MCP contracts, four-shell parity/safety evals, `/api/llm-shells`, and `/llm-shells`. Cursor reported 238/238 tests across 34 files, lint and production build green; GitHub `verify` and Vercel preview are green. The worker is terminal. Live provider installation, marketplace publication, and tenant OAuth remain unverified.
- PR `#141` was merged into `codex/action-shift-122-safe` at merge commit `149703acbb3c8ffbfe0232e3a7eacd99ec197b37` despite the explicit no-merge gate. PRs `#147`, `#148`, and `#151` later advanced production `main` to `567cedd8a250ec1fec335a917b6f09e05adf7851`; `/llm-shells` and its discovery links are now live. Post-merge Codex review found two still-open production truth defects: remove `/llm-shells` from sitemap/crawler/LLM discovery until the stranger door is live, and replace stale `drafted-in-git` or impossible deployment-status copy with durable provider-install/marketplace truth. Live provider UI installation remains unverified. Do not merge the repair without Myke's explicit approval.
- **Grok sales organization** (`bc-01a03102-d4db-752a-ade6-d20e7aae1f71`) is no longer the slot holder. Last seen ~03:58 UTC 26 Aug. Do not count its local parser claims as shipped. Do not relaunch it.
- Grok accepted Codex's P0 Community/four-LLM packet in the command hub. Grok's local PDQ parser produced artifacts, but that parser still has no never86 branch/SHA/PR; it is not shipped.
- The public-safe branch `codex/action-shift-122-safe` now resolves remotely at `27d637b51c45922643b62f123569a0da9c5e1db5` after the `#138` merge. It includes the prior workforce foundation, PDQ yesterday-close, vendor silence clock, and onboarding-security reconciliation. Safe branch only: not production and not live-verified.
- PR `#127` (**Monday gate: one-store activation (#118)**) remains merged into the safe branch, not production. Security correction list still applies before any production activation.
- PR `#131` **merged** onto `codex/action-shift-122-safe` at `a827298` (26 Aug 17:09 UTC). Not production. Prior intake/proof security list still applies before any prod deploy.
- PR `#134` **merged** onto the same safe branch at `f92f89d` (26 Aug 17:37 UTC). Not production.
- Dirty PR `#130` was **CLOSED without merge** at 1:15 PM CT. PR `#132` was also closed without merge. Do not reopen either.
- PR `#135` and PR `#137` are closed as superseded audit history after PR `#138`; never merge either old head. `#138` preserves the unique two-business-date gate and one apply/probe path while fixing the TypeScript/build and onboarding-security defects.

## Open work

- **Shareable Grok Bot swarm — drafted 30 Aug:** Public catalogs have no Never86/86 operator bot. Ranked pack, Product scout role, Cursor automation §7, and API-key notes are on `cursor/never86-grok-shareable-86-swarm-e264`. Adding `x.ai/bot` links to the live desk is a Myke click. No new secrets.
- **Never86 Bolt social-to-order — concept demo done, product proof packet queued:** Grok owns turning `docs/company/intake/2026-08-30-never86-bolt-social-to-order.md` into one bounded beta spec: two restaurants, one orderable hero item each, Reel/deep-link attribution, restaurant-owned guest checkout, explicit order/dispatch/delivery ownership, consented first-party customer data, exception handling, and completed-order/contribution-margin proof. Do not claim native TikTok hot-food checkout, a POS/provider integration, delivery coverage, fee savings, or customer-data ownership until each is contractually and technically proven. Use the current one-code-writer gate; research/spec may run read/draft-only, but no new Cursor launch, partner outreach, public post, spend, merge, deploy, or live credential work follows from this handoff alone.
- **Privacy containment first gate done:** `#162` and `#167` redacted and closed without merge. Do not reopen. Remaining: current-`main` PR for the public handoff redaction + one-seat claim (`610f2c3` is on the old `#166` branch). History rewrite / GitHub Support is a later gate. Do not publish or repeat store-private figures.
- **Overnight one-owner-seat scorecard — closed as missed:** Schedule/Time-Off and bar-week UI shipped, but the promised current-main Google/email one-seat auth activation PR, four-provider smoke receipts, and 7:00 AM evidence card were not produced. Authentication remains fail-closed, provider installs remain unverified, and the brand audit has no proven deliverable. The overnight coordinator is obsolete and should be deleted; ongoing read-only oversight remains with the regular watchtower.
- **Daily Myke Handoff & Tomorrow Brief — workflow defined, execution setup queued:** proposed default cadence is a 5:00 PM CT weekday draft plus a 6:30 AM CT calendar refresh. The Myke master includes what Myke actually did, decisions made, commitments, unfinished items, tomorrow's meetings, attendees, purpose, prep, promises, location/link, and required approvals. Kristin's slice contains company/operator assignments, deadlines, and relevant meetings. Vadim's slice contains product/technical decisions, PR/build/deploy evidence, blockers, and relevant meetings. Every claim needs a source; use `drafted`, `queued`, `tested`, `pushed`, `merged`, `deployed`, and `sent` accurately. Do not expose unrelated CTAP, personnel, customer, financial, or calendar details. Missing calendar access, unverified recipient destinations, conflicting facts, or absent Myke send approval are stop conditions. Required proof: source list, draft timestamp, recipient-specific copy, Myke approval, and delivery receipt.
- **Rolling code queue — one product writer at a time:** one-seat claim + public handoff redaction is committed at `610f2c3` on `cursor/desktop-computer-auth-dcde` (already-merged `#166` branch). Next ship step is one PR off current `main`. No merge, Neon apply, or credential issuance.
- **Live staff-auth activation authorization:** Myke approved the live staff database and the exact CTAP claim flow: `verify identity -> pending CTAP request -> Myke/Tom approval -> assigned role/department/seat -> role-scoped workspace`. The request must surface in one Myke/Tom approval inbox with live-roster matching, approve/reject, role assignment, duplicate-account linking, revoke/reset, and audit history. Verified identity alone grants no tenant membership, role, checklist, schedule, communication, manager, or operator access. The safe sequence is: (1) repair all-role desk onto current main; (2) replace the blocked readiness plane with reviewed tenant-safe signup/claim, pending approval, sessions, revoke/reset, rate limits, CSRF/state/PKCE, immutable audit receipts, and reversible migration/probe; (3) prove Google + email; (4) add X and phone only when provider keys and verified callbacks exist; (5) apply production migration and activate only after exact SHA/CI/preview and preflight evidence. No invitation or credential is sent until the live door is proven.
- **Four-LLM listing queue:** PR `#159` is open/green/mergeable at `dd7e6ad049989257aaf2bd5bf97869dacef53932`. It adds a noindex store-listing packet plus privacy/terms pages and correctly keeps status `not submitted`; it does not publish Never86 in any provider directory. ChatGPT and Claude still require human directory submissions, while Grok/Gemini remain paste/connect flows under the current design. No merge or submission is authorized yet.
- **Manager email finding:** the thread was not an automated Never86 message. Myke emailed Kristin Aduna and Vadym Kozak the attached `CTap_Tom_Kenzy_floor_card.docx`; Kristin replied asking whether it tells managers to pull a report and answer questions. The one-page card assigns Kenzy front/bar/cash-close ownership and Tom kitchen/drivers/late-delivery ownership. It instructs Tom to use the already-observed late-delivery line to ask four cause questions and record proof; it does not ask managers to become report analysts. No reply has been sent.
- **Parallel non-code queues:** integration source research, independent QA/evals, Fort Dodge GTM/Sentia+ drafts, and Victor/media/social research/drafts may run concurrently when they are read-only or draft-only, have no overlapping code edits, and return evidence. Facebook stays deferred. No outreach, posts, DMs, publishing, or CRM writes.
- **Grok Bot access-repair queue:** audit every Bot and Routine for owner, enabled state, schedule/time zone, connector authentication, reachable source, usage availability, last successful result, blocker, and next owner. Return one access map. Use one shared-computer login per service, connector-first access, and supported egress routing when a site rejects the cloud address. Facebook remains deferred and untouched.
- **Store floor (CTAP Tuesday 8/25):** close already ran. One move is late deliveries; owner is driver lead + closer; proof is tonight’s late line. Factory does not run the drivers. Keep those dollars out of GTM/AEO. Numbers live only in `GROK_HANDOFF.md`.
- **Priority 1 factory — one-seat staff-auth activation:** after verifying every product worker terminal, launch exactly one current-main PR for canonical one-human/one-seat identity plus the locked CTAP flow: Google/email verification -> pending request/no private data -> one Myke/Tom approval inbox -> live-roster match and role/department/seat assignment or rejection -> role-scoped workspace only after approval. Require tenant isolation, duplicate-account linking, identifier re-verification, approve/reject, revoke/reset, rate limits, CSRF/state/PKCE, immutable audit, and reversible dependency-ordered migration/preflight/probes. Stop at a green PR with exact evidence; no merge, live credential issuance, or database apply.
- Do not issue staff credentials through the current operator credential model; it grants full operator-level access. Manager-only comes first, followed by seat-scoped staff authentication and authorization.
- **PR #127 security correction before any production activation:** fail closed in production when activation email delivery is unavailable and never return a raw activation token; rate-limit activation by normalized email and trusted client IP plus add login throttling; HTML-escape every user-supplied email field; consume each activation token atomically in a database transaction/conditional update; and do not fall through from an existing Neon identity with a bad password to a different OPS credential sharing the email.
- **Safe-branch intake/proof security still applies before production:** `/api/intake/email` must not trust spoofable JSON `from`/`to`; require a verified provider webhook signature/secret and signed tenant alias; add payload caps; composite tenant FKs; tenant-bind proof `closeId`; wrap writes in transactions with idempotency. A sender string equal to `pdqreports@pdqpos.com` is not authentication.
- Do not paste `DATABASE_URL` into Grok, Cursor chat, GitHub, or a PR. Live Neon apply requires approved secret storage plus a reviewed migration and probe. The current production onboarding `503` remains a blocker, not permission to bypass the security gates above.
- **Current system context** is archived. Cap stays at `1`; current worker state must be reconciled from `bc-29f78ca2-efd6-4919-8a88-bd1bc18e7874`, `bc-e5e0fb71-57e5-4e0b-8b04-1324c758012f`, and `bc-9181f277-5c45-43ff-af9a-1c898c9bdcde` before any new product dispatch. The stale PR-152 Cursor thread's Gmail blocker is irrelevant because Codex already completed the email review. Never resume the archived Agent-ctap-marketing thread.
- Grok reconciled worker `bc-01a034a9-80d3-7724-8126-f5b10dd8879b` / `run-c3abe9eb` through the live connector at 10:13 PM CT. It is IDLE/FINISHED, its visible result is already merged as PR `#151`, and no unique unpushed work was found. The hub has no archive tool, so leave it idle and do not revive it. No new secrets are needed.
- At 10:13 PM CT Grok accepted Myke's confirmed stall-recovery order, proved `cursor_list_agents` works from the hub, found the bridge healthy, avoided a duplicate launch, and kept `bc-4db471f9-3d4d-4b50-adcd-45651ad228ab` as the sole RUNNING writer. The next checkpoint is its pushed PR, exact SHA, test/build/CI/preview evidence, and a production-applicable base/retarget plan. No merge, deploy, Neon apply, secret handling, CRM/social write, Facebook work, or external send is authorized.
- After the jam clears, Grok owns instantiating the organization above as bounded queues. Start with one code worker plus parallel read-only/draft owners for integration research, QA/evals, GTM/Sentia+, and Victor/media. Scale code concurrency only after the three-clean-job reliability gate.
- After AEO, optional `never86-bridge-smoke-v1`: one harmless documentation-only change from Grok, isolated branch, tests, push/PR evidence, no merge or deploy.
- Facebook is deferred by Myke. Leave its Grok Bot untouched; no agent types or sees a password/2FA. Revisit the login and weekday pack only when Myke asks.
- Enable and verify the daily 7:00 AM America/Chicago draft-only clip Routine in the actual `X as Myke` Grok Bot using `docs/company/social/GROK_ROUTINE_X_DAILY_DRAFT.md`. Required evidence: exact workspace, enabled state, schedule/time zone, last trigger, three-creative output card, and playable export proof. Do not publish. TikTok stays paused until Myke separately changes its cadence.
- Keep one reliable response loop: no duplicate founder chats, no silent/stuck generations, and one blunt result back to Grok. Preserve staged workforce files until reviewed; do not deploy or apply their database migration automatically.
- Continue Action Shift from the public-safe branch `codex/action-shift-122-safe`. Verified remote HEAD: `27d637b51c45922643b62f123569a0da9c5e1db5` after `#138`. Do not merge the old private-heavy branch wholesale. Do not revive dirty `#130`.
- Assign independent agents useful work while the primary builder continues: one owner for tenant/workforce/checklist schema and RLS, one for UI/workflows, one for verification and evidence, and one for integration research. Avoid duplicate edits and require each owner to return evidence.
- Build an integration knowledge program for major restaurant POS, scheduling, and checklist systems and a four-model provider layer. First produce a current vendor matrix, evidence sources, auth/data limits, skill boundaries, and evals; do not invent supported integrations or train on private customer data without an approved data path.
- Integration research is complete enough to sequence the first adapters: POS P0 Toast/Square/Clover; scheduling P0 7shifts/Homebase/Deputy; checklists P0 SafetyCulture/MaintainX; invoices/food cost MarginEdge first when present, otherwise approved CSV/PDF/email intake. CTAP's actual provider names and plan levels still must be confirmed before calling any connector live.
- Current official four-LLM finding: there is no universal marketplace manifest or one-click install. Use one HTTPS Streamable-HTTP MCP gateway with OAuth 2.1/PKCE, provider-specific client registrations, server-side read/draft/commit scopes, idempotency, and immutable audit. Package thin OpenAI, Claude, Gemini, and Grok shells around the same versioned tools and skill source; certify read-only first, then drafts, then controlled writes.
- Fort Dodge is the recommended first market. Keep interview candidates separate from sales leads: inventory 40 public directory entries, research 12 independents, approve five, and invite three to five only after Myke approves the exact destinations/copy. Victor owns interview quality/recording; bots prepare source-backed dossiers, briefs, clip candidates, and drafts; humans send and publish.
- Finish and verify the Grok Social Command org and automations.
- Deploy the newer company-org/hunter MCP tools when the branch is ready and Git authentication is fixed.
- Keep Gmail/Calendar cloud OAuth failures separate from the core HQ workflow; local capabilities may still work.
- **Desktop/Chrome standing order — recorded, not executed:** cloud worker `bc-9181f277-5c45-43ff-af9a-1c898c9bdcde` verified Google Drive/Mobbin/Render/Supabase ready and Gmail/Calendar/X `needsAuth`. Next human clicks are Cursor desktop MCP Connect for those three (not Apollo), then Grok Bot Agent Computer takeover only at login/2FA/CAPTCHA. No accounts created this run.
- Do not authorize broad Supabase or X write/billing scopes without a new permission review.

## Next handoff rule

Replace or extend this file with the newest durable facts. Keep superseded decisions only when the history matters. Never treat this file as authorization for an external send or write.
