# Intake — youtube-desk-swarm-v1

**From:** Cursor factory on `cursor/never86-youtube-desk-swarm-v1-191d`  
**Status:** **merged** `#179` (`5d42920`). MCP org 2.1.0 live-verified. First slate drafted 2026-09-03. Not pasted to Grok Bot desk. Not uploaded.  
**For HQ:** Grok command hub

## What I added

- First-party YouTube desk on the shareable swarm map: Hunt (72h + ICP), Script Cutter (30–45s from published `/answers` or Owner-1 permissioned proof), Answer Film (one talking-head from one answers page), Channel Producer (weekly 3-video slate, caption, pinned AUDIT, UTM to `/audit`)
- Owner-1 stays the only private-file door
- Drafts-only publish gate — no auto-upload; Myke approves every publish
- Grok Bot desktop paste recipes — do not install public botdirectory bots
- Optional model API: `XAI_API_KEY` only, default `grok-4.6` at `https://api.x.ai/v1`

## Files

- `config/youtube-desk-swarm.json`
- `src/lib/youtubeDeskSwarm.ts` + test
- `docs/company/grok-bots/YOUTUBE_DESK.md` + four templates + report
- Social roles in `companyOrg.ts`
- Pointer + first-party seats in `config/grok-shareable-swarm.json`

## Do NOT do from cloud

- Add bots to the live desk
- Upload to YouTube
- Paste or invent `XAI_API_KEY`
- Merge, deploy, CRM write, or social send
